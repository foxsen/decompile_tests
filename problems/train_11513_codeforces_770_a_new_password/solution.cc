#include <bits/stdc++.h>
using namespace std;
long long i, j, n, k, l, m, r, x, y, K;
string s, t, s1;
int main() {
  cin >> n >> m;
  for (i = 0; i < n; i++) s.push_back('a' + i % m);
  cout << s << endl;
  return 0;
}
