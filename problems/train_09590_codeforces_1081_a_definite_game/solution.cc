#include <bits/stdc++.h>
using namespace std;
const int N = 1e6 + 100;
int ans[20002];
int main() {
  ios::sync_with_stdio(0);
  cin.tie(0);
  int n;
  cin >> n;
  if (n == 2) {
    cout << 2 << '\n';
  } else {
    cout << 1 << '\n';
  }
  return 0;
}
